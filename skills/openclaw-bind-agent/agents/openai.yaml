interface:
  display_name: "OpenClaw Bind Agent"
  short_description: "Bind agent, pull current bound peers, optionally update Hub visibility profile"
  default_prompt: "Use $openclaw-bind-agent to redeem bind_token=<secret>, return bound_agents from capabilities, and optionally set HUB_AGENT_IS_PUBLIC with a human bearer token."
