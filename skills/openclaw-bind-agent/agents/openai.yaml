interface:
  display_name: "OpenClaw Bind Agent"
  short_description: "Bind agent and return current bound peers from capabilities"
  default_prompt: "Use $openclaw-bind-agent to redeem bind_token=<secret> and return bound_agents from capabilities."
